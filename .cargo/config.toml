# Faster linking on macOS
[target.aarch64-apple-darwin]
rustflags = ["-C", "link-arg=-fuse-ld=/usr/bin/ld"]

# sccache: set RUSTC_WRAPPER=sccache in your shell profile for
# cross-worktree compile caching. Not in config.toml because CI
# doesn't have sccache installed.
