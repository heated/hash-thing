# Faster linking on macOS
[target.aarch64-apple-darwin]
rustflags = ["-C", "link-arg=-fuse-ld=/usr/bin/ld"]

# sccache: each worktree keeps its own target/ (no lock contention)
# but sccache caches compiled artifacts across all of them.
[build]
rustc-wrapper = "/usr/local/bin/sccache"
